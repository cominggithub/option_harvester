# Copy to .env (prod) and .env.test (test). See CLAUDE.md "Database" section.
# Local PostgreSQL, peer auth over unix socket, user "coming", no password.
DATABASE_URL="postgresql://coming@localhost/option_harvester?host=/var/run/postgresql&schema=public"
PORT=19210

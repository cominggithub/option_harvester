# Strategy session — <YYYY-MM-DD> · <slug>

**Saved:** <ISO timestamp> · **HEAD:** `<short sha>` · **Rules version:** `<CURRENT_VERSION>`
· **Record as of this save:** <legs> closed legs / <chains> chains, realized <$>, from
`npm run reconcile:sc` on <date>.

> Every number below is a **snapshot**. Re-measure before quoting: `npm run reconcile:sc`, or
> the `/md/*.md` mirrors of `/risk`, `/short-call`, `/short-call/losses`.

## 1. What this session was about

One or two sentences. The question, not the activity.

## 2. Measured this session

| Finding | Number | n | Source | Date |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 3. Open threads

For each: the hypothesis, what would confirm or kill it, the `n` still needed, and where the
data would come from. A thread with no test is not a thread — it is an opinion, and it belongs
in §5.

| # | Hypothesis | Test | n needed | Blocked by |
| --- | --- | --- | --- | --- |
| 1 |  |  |  |  |

## 4. Rejected — do not re-propose without new data

| Idea | Why it was killed | Evidence |
| --- | --- | --- |
|  |  |  |

## 5. Doctrine changes proposed or landed

Version bumps, rule ids touched, and whether the spec changelog and `sc-rules.ts` both moved
(`npm run check` fails when they disagree).

## 6. Next actions, in order

1.

## 7. What I could not verify, and why

The standing caveats live in [../system-gaps.md](../system-gaps.md) — cite the section rather
than repeating it. List here only what was *newly* unverifiable this session.
